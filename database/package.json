{
  "name": "database",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "mkdir -p public && json-server --watch db.json --host 0.0.0.0 --port $PORT --static public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "description": ""
}
